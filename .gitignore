/vendor
/composer.lock
/.phpunit.cache
/.phpstan.cache
.DS_Store
/build
/.superpowers
# A local working directory for build-time plans, not part of the package.
# Tracking one of those once put 19 copies of a 470 KB file into history, 45% of
# the repository's objects. The reasoning worth keeping lives in docs/design.md,
# which is tracked; everything here is scratch.
/docs/superpowers/
